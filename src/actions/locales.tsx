import { createAction } from 'redux-actions';

export const changeLang = createAction('change lang');
export const logout = createAction('logout');
export const updateUserConfig = createAction('update user config');
