import { createAction } from 'redux-actions';

export const getTos = createAction('get tos');
export const getSpotBalance = createAction('get spot usd balance');
export const agreeTos = createAction('agree tos');
